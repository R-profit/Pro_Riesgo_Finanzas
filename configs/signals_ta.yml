symbols: ["GC=F"]
period: "1y"

strategy:
  name: "bot_runner.strategies.signals_ta:SignalsTaStrategy"
  params:
    ema_fast: 12
    ema_slow: 26
    rsi_buy: 55
    rsi_sell: 45
    bb_window: 20
    bb_dev: 2.0
    width_thresh: 0.10
    buy_threshold: 0.65
    sell_threshold: 0.60

execution:
  plot: true
  export_dir: "outputs"

risk:
  cash: 10000

